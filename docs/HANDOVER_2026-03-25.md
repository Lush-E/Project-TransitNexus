# Handover Memo (2026-03-25)

This document summarizes what was completed and what to continue next.

## Recently Completed

### 1) Platform and Fill Behavior

- Platform drawing switched to segment-chain style
- Loop fill detection added and improved with tolerant endpoint grouping
- Platform cursor marker added for better drafting visibility

### 2) Save / Load UX

- Title menu flow updated:
  - New Game
  - Save data select (auto-load after file pick)
  - Continue (auto-load last selected file, fallback to quick save)
  - Exit
- Save status chip added in header for quick save/load feedback

### 3) Track and Selection

- Minimum track length baseline changed to 1.0u
- Selection improved to nearest centerline style
- Track dead-end marker added and tuned:
  - length +/-1.5u
  - width synced to platform width
  - color follows track color

### 4) Dead-End False Positive Fix

- Dead-end logic no longer relies on endpoint-only matches
- Endpoint touching another segment interior is now treated as connected

### 5) Save Data Compatibility

- Save schema bumped to version 5
- Load-time normalization now includes:
  - track level/lineType/color normalization
  - topology normalization for near-endpoint merge and interior branch snap
- Sample save updated:
  - save/transitnexus-2026-03-20-08-50-01.json -> version 5

## Current Behavior to Remember

- Branches drawn into the middle of a track should not show dead-end bars
- Continue button preference:
  1. last selected save in localStorage
  2. quick save in localStorage
- Quick save is localStorage-based and does not produce a file

## Suggested Next Steps

1. Build internal graph model for train routing
2. Split crossing/intersection points into explicit graph nodes
3. Implement route finding and branch choice for trains
4. Add optional graph debug render (nodes/edges IDs)

## Verification Checklist (Quick)

- [ ] New Game starts with 1.0u min track length
- [ ] Save data select auto-loads selected JSON
- [ ] Continue loads last selected save or QSV fallback
- [ ] Mid-segment branches do not show dead-end markers
- [ ] Dead-end bars render with track color and +/-1.5u length
- [ ] No console errors on load/save operations

## Notes

- Working environment is Windows + Electron
- If reflection appears stale, restart app process and re-open project state
